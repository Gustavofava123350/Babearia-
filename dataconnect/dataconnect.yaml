specVersion: "v1beta"
serviceId: "ellys"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ellys-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
